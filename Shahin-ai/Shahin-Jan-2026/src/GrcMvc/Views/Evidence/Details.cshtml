@using Microsoft.AspNetCore.Mvc.Localization
@inject IHtmlLocalizer<SharedResource> L
@model GrcMvc.Models.DTOs.EvidenceDto
@{
    ViewData["Title"] = L["Evidence_Details_Title"].Value;
    Layout = "_Layout";
}

<div class="container-fluid">
    <div class="row mb-3">
        <div class="col">
            <h2><i class="bi bi-file-earmark-text"></i> @L["Evidence_Details_Title"]</h2>
        </div>
        <div class="col text-end">
            <a href="@Model.Location" target="_blank" class="btn btn-success">
                <i class="bi bi-download"></i> @L["Evidence_Download"]
            </a>
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">
                <i class="bi bi-pencil"></i> @L["Edit"]
            </a>
            <a asp-action="Index" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> @L["BackToList"]
            </a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row">
        <div class="col-md-8">
            <div class="card mb-4">
                <div class="card-header bg-light">
                    <h5 class="mb-0">@L["Evidence_GeneralInformation"]</h5>
                </div>
                <div class="card-body">
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label class="fw-bold">@L["Name"]</label>
                            <p>@Model.Name</p>
                        </div>
                        <div class="col-md-6">
                            <label class="fw-bold">@L["Status"]</label>
                            <p>
                                <span class="badge @GetStatusBadgeClass(Model.Status)">
                                    @Model.Status
                                </span>
                            </p>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <div class="col-md-4">
                            <label class="fw-bold">@L["Type"]</label>
                            <p>@Model.EvidenceType</p>
                        </div>
                        <div class="col-md-4">
                            <label class="fw-bold">@L["Evidence_Classification"]</label>
                            <p>
                                <span class="badge @GetClassificationClass(Model.DataClassification)">
                                    @Model.DataClassification
                                </span>
                            </p>
                        </div>
                        <div class="col-md-4">
                            <label class="fw-bold">@L["Evidence_Source"]</label>
                            <p>@Model.Source</p>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="fw-bold">@L["Description"]</label>
                        <p>@Model.Description</p>
                    </div>
                    <div class="mb-3">
                        <label class="fw-bold">@L["Evidence_Location"]</label>
                        <p class="text-break"><a href="@Model.Location" target="_blank">@Model.Location</a></p>
                    </div>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header bg-light">
                    <h5 class="mb-0">@L["Evidence_Metadata"]</h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="fw-bold">@L["Evidence_Tags"]</label>
                        <p>
                            @if (!string.IsNullOrEmpty(Model.Tags))
                            {
                                foreach(var tag in Model.Tags.Split(','))
                                {
                                    <span class="badge bg-secondary me-1">@tag.Trim()</span>
                                }
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </p>
                    </div>
                    <div class="mb-3">
                        <label class="fw-bold">@L["Evidence_Notes"]</label>
                        <p>@(string.IsNullOrEmpty(Model.Notes) ? "-" : Model.Notes)</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card mb-4">
                <div class="card-header bg-light">
                    <h5 class="mb-0">@L["Evidence_ValidityOwnership"]</h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="fw-bold">@L["Owner"]</label>
                        <p>@Model.Owner</p>
                    </div>
                    <hr />
                    <div class="mb-3">
                        <label class="fw-bold">@L["Evidence_CollectionDate"]</label>
                        <p>@Model.CollectionDate.ToShortDateString()</p>
                    </div>
                    <div class="mb-3">
                        <label class="fw-bold">@L["Evidence_ExpirationDate"]</label>
                        <p class="@(Model.ExpirationDate < DateTime.UtcNow ? "text-danger fw-bold" : "")">
                            @(Model.ExpirationDate?.ToShortDateString() ?? "-")
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@functions {
    private string GetStatusBadgeClass(string status)
    {
        return status?.ToLower() switch
        {
            "active" or "valid" => "bg-success",
            "expired" => "bg-danger",
            "pending" => "bg-warning",
            "archived" => "bg-secondary",
            _ => "bg-info"
        };
    }

    private string GetClassificationClass(string classification)
    {
        return classification?.ToLower() switch
        {
            "public" => "bg-success",
            "internal" => "bg-info",
            "confidential" => "bg-warning text-dark",
            "restricted" => "bg-danger",
            _ => "bg-secondary"
        };
    }
}
