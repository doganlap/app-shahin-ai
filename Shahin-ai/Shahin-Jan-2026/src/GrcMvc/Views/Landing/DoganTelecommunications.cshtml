@{
    ViewData["Title"] = "Telecommunications Engineering Consulting - Dogan Consult";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Telecommunications Engineering Consulting - Network architecture, fiber advisory, connectivity planning, and implementation governance." />
    <title>@ViewData["Title"]</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
    
    <style>
        /* CSS Variables removed - now using centralized theme from landing.css */
            --text: #334155;
            --text-muted: #64748b;
            --border: #e2e8f0;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Inter', sans-serif;
            background: #ffffff;
            color: var(--text);
            line-height: 1.7;
        }

        .container { max-width: 1100px; margin: 0 auto; padding: 0 2rem; }

        /* Header */
        .header {
            background: var(--primary);
            padding: 1rem 0;
            position: fixed;
            top: 0; left: 0; right: 0;
            z-index: 100;
        }

        .header-content { display: flex; justify-content: space-between; align-items: center; }

        .logo {
            color: white;
            font-size: 1.4rem;
            font-weight: 700;
            text-decoration: none;
        }

        .logo span { color: var(--accent); }

        .nav { display: flex; gap: 2rem; }

        .nav a {
            color: rgba(255,255,255,0.9);
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .nav a:hover { color: var(--accent); }

        /* Hero */
        .hero {
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
            padding: 9rem 0 5rem;
            color: white;
        }

        .hero-breadcrumb {
            font-size: 0.9rem;
            opacity: 0.8;
            margin-bottom: 1rem;
        }

        .hero-breadcrumb a {
            color: white;
            text-decoration: none;
        }

        .hero-breadcrumb a:hover { color: var(--accent); }

        .hero h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
        }

        .hero p {
            font-size: 1.15rem;
            line-height: 1.8;
            max-width: 700px;
            opacity: 0.95;
        }

        /* Content */
        .section { padding: 4rem 0; }
        .section-alt { background: #f8fafc; }

        .section-title {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 1.5rem;
        }

        .content-text {
            font-size: 1.05rem;
            line-height: 1.9;
            color: var(--text);
            max-width: 800px;
        }

        .content-text p { margin-bottom: 1.5rem; }

        /* Services Grid */
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .service-card {
            background: white;
            border: 1px solid var(--border);
            border-radius: 10px;
            padding: 1.75rem;
        }

        .service-card h4 {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-size: 1.1rem;
            color: var(--dark);
            margin-bottom: 0.75rem;
        }

        .service-card h4 i {
            color: var(--primary);
            font-size: 1.25rem;
        }

        .service-card p {
            font-size: 0.95rem;
            color: var(--text-muted);
            margin: 0;
        }

        /* Standards */
        .standards-list {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
            margin-top: 1.5rem;
        }

        .standard-tag {
            background: var(--primary);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            font-size: 0.9rem;
            font-weight: 500;
        }

        /* CTA */
        .cta-section {
            background: var(--dark);
            padding: 4rem 0;
            text-align: center;
            color: white;
        }

        .cta-section h2 {
            font-size: 1.75rem;
            margin-bottom: 1rem;
        }

        .cta-section p {
            opacity: 0.8;
            margin-bottom: 2rem;
        }

        .cta-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.875rem 2rem;
            background: var(--accent);
            color: var(--dark);
            font-weight: 600;
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.3s;
        }

        .cta-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 20px rgba(201, 162, 39, 0.4);
        }

        /* Footer */
        .footer {
            background: var(--primary);
            color: white;
            padding: 2rem 0;
            text-align: center;
        }

        .footer a {
            color: var(--accent);
            text-decoration: none;
        }

        @@media (max-width: 768px) {
            .hero h1 { font-size: 1.75rem; }
            .nav { display: none; }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="container">
            <div class="header-content">
                <a href="/dogan-consult" class="logo">DOGAN<span>CONSULT</span></a>
                <nav class="nav">
                    <a href="/dogan-consult">Company</a>
                    <a href="/dogan-consult/data-centers">Data Centers</a>
                    <a href="/dogan-consult/cybersecurity">Cybersecurity</a>
                    <a href="/contact">Contact</a>
                </nav>
            </div>
        </div>
    </header>

    <!-- Hero -->
    <section class="hero">
        <div class="container">
            <div class="hero-breadcrumb">
                <a href="/dogan-consult">Dogan Consult</a> / Telecommunications Engineering
            </div>
            <h1>Telecommunications Engineering Consulting</h1>
            <p>
                Dogan Consult provides expert telecommunications engineering advisory services 
                across access, aggregation, and core network environments. We support governments, 
                operators, and enterprises in designing scalable, secure, and resilient telecom infrastructure.
            </p>
        </div>
    </section>

    <!-- Overview -->
    <section class="section">
        <div class="container">
            <h2 class="section-title">Our Approach</h2>
            <div class="content-text">
                <p>
                    Our services include network architecture review, fiber and structured cabling advisory, 
                    connectivity planning, and implementation governance. We ensure designs align with 
                    international standards, operational requirements, and long-term capacity planning.
                </p>
                <p>
                    We work closely with stakeholders to assess requirements, design robust architectures, 
                    and assure quality, security, and long-term resilience throughout delivery.
                </p>
            </div>
        </div>
    </section>

    <!-- Services -->
    <section class="section section-alt">
        <div class="container">
            <h2 class="section-title">Services</h2>

            <div class="services-grid">
                <div class="service-card">
                    <h4><i class="bi bi-diagram-3"></i> Network Architecture Review</h4>
                    <p>Assessment of network designs for scalability, resilience, and alignment with operational requirements.</p>
                </div>
                <div class="service-card">
                    <h4><i class="bi bi-ethernet"></i> Fiber & Structured Cabling</h4>
                    <p>Advisory on fiber optic and structured cabling infrastructure design and implementation.</p>
                </div>
                <div class="service-card">
                    <h4><i class="bi bi-globe"></i> Connectivity Planning</h4>
                    <p>Strategic planning for network connectivity across access, aggregation, and core environments.</p>
                </div>
                <div class="service-card">
                    <h4><i class="bi bi-clipboard-check"></i> Implementation Governance</h4>
                    <p>Oversight and quality assurance throughout network deployment and commissioning phases.</p>
                </div>
                <div class="service-card">
                    <h4><i class="bi bi-graph-up"></i> Capacity Planning</h4>
                    <p>Long-term capacity assessment and growth planning for network infrastructure.</p>
                </div>
                <div class="service-card">
                    <h4><i class="bi bi-shield-check"></i> Security Assessment</h4>
                    <p>Network security review and recommendations aligned with industry best practices.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Standards -->
    <section class="section">
        <div class="container">
            <h2 class="section-title">Standards Alignment</h2>
            <div class="content-text">
                <p>
                    Our telecommunications engineering services are aligned with international standards 
                    and best practices to ensure consistency, quality, and interoperability.
                </p>
            </div>
            <div class="standards-list">
                <span class="standard-tag">ITU-T</span>
                <span class="standard-tag">3GPP</span>
                <span class="standard-tag">IEEE 802</span>
                <span class="standard-tag">TIA/EIA</span>
                <span class="standard-tag">ISO/IEC 11801</span>
                <span class="standard-tag">MEF</span>
            </div>
        </div>
    </section>

    <!-- CTA -->
    <section class="cta-section">
        <div class="container">
            <h2>Discuss Your Telecommunications Requirements</h2>
            <p>Contact us to explore how we can support your network infrastructure initiatives.</p>
            <a href="/contact" class="cta-btn">
                <i class="bi bi-envelope"></i>
                Get in Touch
            </a>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p><a href="/dogan-consult">DOGAN CONSULT</a> — ICT & Telecommunications Engineering Consulting</p>
        </div>
    </footer>
</body>
</html>
